# Runic Name 视觉 QA

## 运行证据

- 390×844：长中英混合用户名、幽灵手指真实推进、点云符文与标题安全区通过。
- 320×568：标题、主符文、姓名与底部进度均未裁切。
- `?baseline=1`：24 张原作纹理加载完成，产品 UI 隐藏。
- 触控：单指上拖后归一化进度从 0 增长，幽灵手指立即退场。
- 身份：平台外与空上下文显示 `AlterU`；源码使用规范 AlterU 资料接口，
  不含 `AW.PROFILE.GET` 或 GitHub 用户名回退。

## 修复记录

1. 首轮标题位于访客栏后方；移动端顶边从 26px 调整为 86px，小屏为 72px。
2. 完成判定从 99.5% 收紧到视觉已稳定的 98.5%，避免摩擦尾段延迟结算。

## 结论

无 P0/P1。原作点云、旋转与拖动保持可辨认，产品身份、触屏引导、结算和窄屏
构图已完成。
